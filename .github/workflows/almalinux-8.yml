---
name: AlmaLinux 8

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main
      - master
  pull_request:
    branches: []

jobs:
  ansible-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Obtain sources
        uses: actions/checkout@v3
      - name: Test Galaxy role
        run: >
            CONT_ID=$(podman run --rm -v ${{ github.workspace }}:/repo -v /sys/fs/cgroup:/sys/fs/cgroup:ro
            --tmpfs /tmp --tmpfs /run --privileged --detach bviktor/ansible-systemd-almalinux:8) &&
            podman exec ${CONT_ID} /bin/bash -c
            "if [ -f requirements.yml ]; then ansible-galaxy role install --force -r requirements.yml -p ..; fi &&
            dnf -y install firewalld && systemctl start firewalld.service &&
            ./install.sh --extra-vars 'ANSIBLE_ROLES_PATH=.. ANSIBLE_FORCE_COLOR=true skip_kfgame=true' <<< y &&
            ./uninstall.sh --extra-vars 'ANSIBLE_ROLES_PATH=.. ANSIBLE_FORCE_COLOR=true' <<< y &&
            ./install.sh --extra-vars 'ANSIBLE_ROLES_PATH=.. ANSIBLE_FORCE_COLOR=true skip_kfgame=true kf2_classic=true' <<< y" &&
            podman stop ${CONT_ID}
